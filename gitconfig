[user]
	email = rupanshu.ganvir@gmail.com
	name = Rupanshu Ganvir
[core]
	editor = vim
[alias]
	st = status
	cl = clone
    co = checkout
	cmt = commit
	cp = cherry-pick
	pushpls = push --force-with-lease
    current = rev-parse --abbrev-ref HEAD
    pushfresh = !CURRENT=$(git current) && git push --set-upstream origin $CURRENT
[push]
	autoSetupRemote = true
[pull]
    ff = only
[branch]
	sort = -committerdate
